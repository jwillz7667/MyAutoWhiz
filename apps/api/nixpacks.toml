# Nixpacks configuration for API service
# Railway should set "Root Directory" to repo root in dashboard,
# then use this config with NIXPACKS_CONFIG_FILE=apps/api/nixpacks.toml

[phases.setup]
nixPkgs = ["nodejs_20", "pnpm", "openssl"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm --filter @myautowhiz/shared run build", "pnpm --filter @myautowhiz/api run build"]

[start]
cmd = "node apps/api/dist/index.js"
