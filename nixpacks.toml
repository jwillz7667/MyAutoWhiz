# Nixpacks configuration for Railway deployments
[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["cd apps/api && pnpm prisma generate", "pnpm build"]

[start]
cmd = "node dist/index.js"
